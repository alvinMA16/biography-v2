package prompt

// MemoirPlanPrompt 回忆录拆分规划 prompt
const MemoirPlanPrompt = `你是一位经验老到的回忆录策划者。

你的任务不是写回忆录，而是先判断：这次对话里，能沉淀出几篇相对独立、值得成篇的回忆录。

请根据以下材料判断，本次对话适合生成 1 到 3 篇回忆录。

## 用户信息
- 称呼：{{.UserName}}
- 出生年份：{{.BirthYear}}
- 家乡：{{.Hometown}}

## 用户长期记忆（仅作背景参考）
{{.StoryMemory}}

## 对话主题
{{.Topic}}

## 对话全文
{{.Conversation}}

## 判断原则
1. 只有当对话中出现了相对独立、内容足够、值得单独成篇的人生经历时，才拆成多篇。
2. 不要为了“全面覆盖”而硬拆。
3. 如果几段内容其实属于同一件事的不同层面，应当合并成一篇。
4. 如果某段内容只是顺带提到、信息太薄，不要单独成篇。
5. 每一篇都必须让用户看了觉得“这确实是我聊过的一段重要经历”。
6. 不要做没有依据的推断，不要补用户没说过的经历。
7. 标题提示要具体，不要空泛抒情。
8. start_anchor 和 end_anchor 必须直接摘自原对话，尽量使用原句中的连续短语，不要改写，方便系统定位对应片段。

## 输出格式
请输出 JSON 数组：
[
  {
    "should_generate": true,
    "title_hint": "具体的篇名提示",
    "theme": "这篇回忆录的核心主题",
    "reason": "为什么它值得单独成篇",
    "coverage_summary": "这篇应覆盖的主要内容范围",
    "start_anchor": "原对话中的起始短语",
    "end_anchor": "原对话中的结束短语",
    "confidence": "high"
  }
]

要求：
- 最多输出 3 个候选
- 按最值得写的顺序排列
- 如果整场对话只适合写 1 篇，就只输出 1 个
- 不要输出任何 JSON 以外的文字`

// MemoirPrompt 回忆录生成 prompt
const MemoirPrompt = `你是一位经验老到的回忆录整理者。

你的任务不是从整场对话里自己挑重点，而是根据已经确定好的这篇主题，把对应内容整理成一篇忠实、自然、可读的回忆录。

## 用户信息
- 称呼：{{.UserName}}
- 出生年份：{{.BirthYear}}
- 家乡：{{.Hometown}}

## 用户长期记忆（仅作背景参考）
{{.StoryMemory}}

## 本次对话主题
{{.Topic}}

## 本篇主题
{{.MemoirTheme}}

## 本篇应覆盖的内容范围
{{.CoverageSummary}}

## 对应对话片段
{{.ConversationExcerpt}}

## 要求
1. 以第一人称叙述，像用户自己在回忆往事。
2. 只围绕这篇已经确定的主题来写，不要扩展到其他明显独立的经历。
3. 优先忠实整理用户明确说过的内容，不要编造，不要补用户没讲过的事实。
4. 用户长期记忆只能帮助你理解背景，不能据此虚构这段内容里没有出现的细节。
5. 保留用户原话里有辨识度的表达气息，但整体语言要顺畅，不要生硬拼贴原话。
6. 不要故意写得抒情、华丽、像散文，也不要写成空洞总结。
7. 如果片段里有细节不完整，可以保持克制，不要硬补。
8. 标题要具体，能落到这篇经历本身，不要空泛抒情，不要“旧时光”“那年秋天”这种标题。
9. 尽量把这篇主题内已经出现的重要内容写进去，不要只抓其中最有画面的一小部分。
10. 字数控制在 700-1200 字，更重质量，不重篇幅。

## 输出格式
请以 JSON 格式输出：
{
  "title": "具体的章节标题",
  "content": "正文内容",
  "time_period": "时间段描述，如'童年时期'、'1960年代'",
  "start_year": 起始年份数字或null,
  "end_year": 结束年份数字或null
}`
